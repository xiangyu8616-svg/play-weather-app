# 玩天气 App - 技术架构评估报告

## 一、现有代码结构分析

### 1.1 项目架构概览

```
play-weather-app/
├── app/
│   └── (tabs)/
│       ├── index.jsx          # 首页 - 地球仪主界面
│       ├── forecast.jsx       # 预报页面 - 7 天天气趋势
│       ├── community.jsx      # 社区页面
│       ├── profile.jsx        # 个人中心
│       └── _layout.jsx        # Tab 导航布局
├── components/
│   ├── globe/
│   │   └── GlobeView.jsx      # 3D 地球仪组件 (globe.gl + WebView)
│   └── weather/
│       ├── WeatherCard.jsx    # 天气详情卡片
│       └── PhenomenonFilter.jsx # 现象类型筛选
├── services/                  # 【新建】数据服务层
│   ├── weatherService.js      # 和风天气 API 封装
│   ├── auroraService.js       # NOAA 极光数据
│   ├── astronomyService.js    # 天文计算
│   └── phenomenonService.js   # 特殊现象算法
└── package.json
```

### 1.2 GlobeView.jsx 扩展能力评估

**当前实现：**
- 使用 `react-native-webview` 嵌入 globe.gl 库
- 支持点标记 (pointsData) 展示极光等現象
- 自动旋转、缩放交互
- 点击事件通过 `ReactNativeWebView.postMessage` 回传

**扩展点分析：**

| 扩展方向 | 可行性 | 实现方式 |
|---------|--------|---------|
| 多层数据叠加 | ✅ 高 | 使用 `hexBinPointsData` 或 `ringsData` 添加多层可视化 |
| 动态数据更新 | ✅ 高 | 通过 `webViewRef.postMessage()` 注入新数据 |
| 自定义标记样式 | ✅ 中 | 扩展 `pointLabel`、`pointColor` 回调函数 |
| 时间轴动画 | ✅ 中 | 添加 `frame` 参数，逐帧更新数据 |
| 朝霞晚霞热力图 | ✅ 高 | 使用 `hexBinPointsData` + 颜色渐变 |
| 云海等高线 | ✅ 中 | 使用 `contour` 或自定义 shader |

**建议扩展方案：**
```javascript
// 在 GlobeView.jsx 中添加数据更新方法
const updateGlobeData = useCallback((data) => {
  webViewRef.current?.postMessage(JSON.stringify({
    type: 'updateData',
    payload: data
  }));
}, []);

// 在 HTML 中监听消息
window.addEventListener('message', (event) => {
  const data = JSON.parse(event.data);
  if (data.type === 'updateData') {
    globe.pointsData(data.payload.points);
    globe.hexBinPointsData(data.payload.heatmap);
  }
});
```

---

## 二、数据接入架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端层 (React Native + Expo)           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐  │
│  │ GlobeView   │  │ WeatherCard │  │ PhenomenonFilter│  │
│  │ (globe.gl)  │  │             │  │                 │  │
│  └─────────────┘  └─────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                    服务层 (API 封装 + 算法)                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐  │
│  │ weather     │  │ aurora      │  │ astronomy       │  │
│  │ Service     │  │ Service     │  │ Service         │  │
│  │ (和风天气)  │  │ (NOAA)      │  │ (suncalc)       │  │
│  └─────────────┘  └─────────────┘  └─────────────────┘  │
│  ┌─────────────────────────────────────────────────────┐│
│  │              phenomenonService (自定义算法)          ││
│  │  • 朝霞晚霞概率  • 云海预测  • 日照金山条件          ││
│  └─────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                    数据层 (本地缓存 + 远程 API)            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐  │
│  │ AsyncStorage│  │ Zustand     │  │ 远程 API         │  │
│  │ (持久化)    │  │ (状态管理)  │  │ (和风/NOAA)     │  │
│  └─────────────┘  └─────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 2.2 API 数据源

| 数据类型 | 数据源 | 更新频率 | 免费额度 |
|---------|--------|---------|---------|
| 基础天气 | 和风天气 API | 30 分钟 | 300 次/天 |
| 极光预报 | NOAA SWPC | 1 小时 | 免费 |
| 日出日落 | suncalc 计算 | 实时 | 本地计算 |
| 月相 | astronomy-engine | 实时 | 本地计算 |
| 朝霞晚霞 | 自定义算法 | - | - |
| 云海 | 自定义算法 | - | - |

---

## 三、技术选型建议

### 3.1 依赖包推荐

```json
{
  "dependencies": {
    "suncalc": "^1.9.0",
    "astronomy-engine": "^2.1.19",
    "expo-notifications": "~0.29.0",
    "@react-native-async-storage/async-storage": "1.23.1",
    "axios": "^1.6.0",
    "date-fns": "^3.0.0",
    "lodash": "^4.17.21"
  }
}
```

### 3.2 技术选型对比

| 功能 | 推荐方案 | 备选方案 | 理由 |
|-----|---------|---------|------|
| 天文计算 | **suncalc** | astronomy-engine | 轻量 (5KB), 专注日出日落，API 简洁 |
| 月相计算 | **astronomy-engine** | suncalc | 精度更高，支持详细月相 |
| 状态管理 | **zustand** (已有) | Redux | 轻量，支持持久化中间件 |
| 本地缓存 | **AsyncStorage** | WatermelonDB | 简单 KV 存储，Expo 原生支持 |
| HTTP 请求 | **axios** | fetch | 拦截器、取消请求、类型友好 |
| 推送通知 | **expo-notifications** | OneSignal | Expo 原生集成，跨平台 |
| 地图可视化 | **globe.gl** (已有) | Three.js + Mapbox | globe.gl 封装完善，适合全球数据 |

### 3.3 安装命令

```bash
# 核心依赖
npx expo install expo-notifications @react-native-async-storage/async-storage

# 天文计算库
npm install suncalc astronomy-engine

# HTTP 请求和工具库
npm install axios date-fns lodash
```

---

## 四、性能优化建议

### 4.1 地球仪渲染性能

**问题：** 城市标记过多会导致 WebView 卡顿

**优化方案：**

```javascript
// 1. 标记聚类 - 只显示 Top N 个最佳观测点
const topPoints = useMemo(() => {
  return allPoints
    .sort((a, b) => b.probability - a.probability)
    .slice(0, 50); // 只显示前 50 个
}, [allPoints]);

// 2. LOD (Level of Detail) - 根据缩放级别显示不同密度
globe.pointsData(zoomLevel > 3 ? detailedPoints : sparsePoints);

// 3. 使用 hexBinPointsData 替代大量点标记
globe
  .hexBinPointsData(largeDataset)
  .hexBinMerge(true)
  .hexLabel(d => `区域：${d.sum} 个观测点`);

// 4. 禁用不必要的特效
globe
  .showAtmosphere(false)  // 或降低分辨率
  .polygonAltitude(0.01);
```

### 4.2 数据请求频率限制

```javascript
// services/cache.js
const CACHE_TTL = {
  weather: 30 * 60 * 1000,      // 30 分钟
  aurora: 60 * 60 * 1000,       // 1 小时
  astronomy: 24 * 60 * 60 * 1000 // 24 小时 (天文数据变化慢)
};

// 请求节流
const throttleRequests = (fn, delay) => {
  let lastCall = 0;
  return async (...args) => {
    const now = Date.now();
    if (now - lastCall < delay) return;
    lastCall = now;
    return fn(...args);
  };
};
```

### 4.3 离线缓存策略

```javascript
// 使用 Zustand 持久化
import { persist } from 'zustand/middleware';

const useWeatherStore = create(
  persist(
    (set) => ({
      cachedData: {},
      setCachedData: (key, data) => set({ 
        cachedData: { ...cachedData, [key]: data } 
      }),
      getCachedData: (key) => cachedData[key],
    }),
    {
      name: 'weather-storage',
      getStorage: () => AsyncStorage,
      partialize: (state) => ({ cachedData: state.cachedData }),
    }
  )
);
```

### 4.4 图片/资源优化

```javascript
// 1. 使用 WebP 格式
// 2. 压缩地球纹理图片 (建议 1024x512)
// 3. 懒加载非关键资源
// 4. 使用 expo-asset 预加载关键图片

import { Asset } from 'expo-asset';

await Asset.loadAsync([
  require('../assets/earth-texture.webp'),
  require('../assets/earth-topology.webp'),
]);
```

---

## 五、风险评估

### 5.1 技术难点

| 难点 | 风险等级 | 解决方案 |
|-----|---------|---------|
| WebView 与 React Native 通信 | 中 | 建立标准化消息协议，添加重试机制 |
| 地球仪大量数据渲染 | 中 | 使用聚类、LOD、Web Worker 预处理 |
| 多 API 数据融合 | 低 | 统一数据格式，添加适配层 |
| 离线数据一致性 | 中 | 添加版本号，增量更新 |

### 5.2 第三方 API 稳定性

| API | 稳定性 | 备用方案 |
|-----|--------|---------|
| 和风天气 | 高 (99.9%) | 心知天气、OpenWeatherMap |
| NOAA SWPC | 中 (99%) | 本地缓存 + 降级显示 |
| suncalc | 高 (本地库) | 无风险 |

### 5.3 合规风险

**气象数据发布资质：**
- ⚠️ 中国境内发布气象预报需取得《气象信息服务许可证》
- 建议方案：
  1. 与和风天气合作，使用其合规数据
  2. 标注"仅供参考，不作为决策依据"
  3. 避免使用"预报"字眼，改用"预测"、"趋势"

**用户隐私：**
- 位置数据本地处理，不上传服务器
- 遵循《个人信息保护法》

---

## 六、开发路线图

### Phase 1 - 基础服务层 (1 周)
- [ ] 创建 services 目录结构
- [ ] 实现 weatherService (和风天气 API)
- [ ] 实现 astronomyService (日出日落计算)
- [ ] 添加 AsyncStorage 缓存

### Phase 2 - 特色功能 (2 周)
- [ ] 实现 auroraService (NOAA 数据)
- [ ] 实现 phenomenonService (朝霞晚霞算法)
- [ ] 扩展 GlobeView 支持多层数据
- [ ] 添加时间轴动画

### Phase 3 - 优化与测试 (1 周)
- [ ] 性能优化 (渲染、缓存)
- [ ] 离线模式测试
- [ ] 推送通知集成
- [ ] 合规审查

---

## 七、代码骨架文件

以下骨架代码已创建：
- `services/weatherService.js`
- `services/auroraService.js`
- `services/astronomyService.js`
- `services/phenomenonService.js`
- `services/cache.js`

详见各文件注释。

---

**报告生成时间：** 2026-04-22  
**版本：** v1.0  
**状态：** 待评审
